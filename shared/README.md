# Shared Assets

Use this directory for reusable assets that are not tied to a single agent.

Examples:

- shared prompt patterns
- templates used to derive agent-specific variants
- helper scripts that are safe to publish
- notes on how the same concept maps across Codex, Claude Code, and Pi
